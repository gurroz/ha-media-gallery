# Screenshots

HACS README screenshots:

- `browse.png` — folder/media grid
- `lightbox.png` — lightbox / slideshow

The root README.md links these by absolute `raw.githubusercontent.com` URL, because HACS renders
the README inside Home Assistant where relative paths do not resolve.
